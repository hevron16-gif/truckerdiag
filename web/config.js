// Адрес backend. Пустая строка:
//   — localhost → http://localhost:8000
//   — продакшен → тот же хост (если API отдаёт и фронт)
// Отдельный фронт на Render: "https://ВАШ-СЕРВИС.onrender.com"
window.TRUCKERDIAG_API = "";
